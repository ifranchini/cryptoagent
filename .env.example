# CryptoAgent Configuration
# At least one LLM API key is required.
# LiteLLM routes to the correct provider based on the model string.

# --- LLM API Keys (set the ones you need) ---
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
DEEPSEEK_API_KEY=
# For local models via Ollama, no key needed — just run `ollama serve`

# --- Per-Agent Model Overrides (LiteLLM model strings) ---
# CA_RESEARCH_MODEL=openai/gpt-4o-mini
# CA_SENTIMENT_MODEL=openai/gpt-4o-mini
# CA_BRAIN_MODEL=openai/gpt-4o
# CA_TRADER_MODEL=openai/gpt-4o-mini
# CA_MACRO_MODEL=openai/gpt-4o-mini

# --- Asset & Exchange ---
# CA_TARGET_TOKEN=SOL
# CA_EXCHANGE=binance
# CA_ASSET_TYPE=crypto

# --- Execution ---
# CA_EXECUTION_MODE=paper
# CA_INITIAL_CAPITAL=10000.0

# --- Persistence ---
# CA_DB_PATH=data/cryptoagent.db

# --- Reflection ---
# CA_REFLECTION_MODEL=openai/gpt-4o-mini
# CA_REFLECTION_CYCLE_LENGTH=5

# --- Risk Management ---
# CA_MAX_DAILY_LOSS_PCT=5.0
# CA_MAX_DRAWDOWN_PCT=15.0
# CA_VOLATILITY_SPIKE_MULTIPLIER=2.0

# --- On-Chain Data ---
# CA_SOLANA_RPC_URL=https://api.mainnet-beta.solana.com
# CA_DEFILLAMA_BASE_URL=https://api.llama.fi

# --- Social Sentiment ---
# CA_TWITTER_BEARER_TOKEN=       # X API v2 Bearer token (optional)
# CA_TWITTER_SCRAPE_URL=         # Scraping proxy URL (optional, for POC)
# CA_REDDIT_SUBREDDITS=["solana","cryptocurrency"]

# --- Macro Data ---
# CA_FRED_API_KEY=               # FRED API key (free at https://fred.stlouisfed.org/docs/api/api_key.html)

# --- Protocol Fundamentals ---
# No API keys needed. DeFiLlama, Snapshot, and GitHub APIs are free and unauthenticated.
# GitHub unauthenticated rate limit: 60 requests/hour (sufficient for single-cycle runs).
